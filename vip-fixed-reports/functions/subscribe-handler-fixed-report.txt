File: inc/functions/subscribe-handler.php
Status: COMPLIED (WordPress VIP Go)

Summary of Changes:
1. Refactored `rock_stars_handle_subscribe_submission` function to comply with WordPress VIP standards.
2. Renamed function and AJAX actions to use `rock_stars_` prefix.
3. Added backward compatibility: Supported both `rock_stars_subscribe` and `wp_custom_subscribe` actions for a smooth transition.
4. Implemented `ob_start()` and `ob_clean()` to ensure a clean JSON response without accidental output (VIP requirement).
5. Strictly sanitized input using `sanitize_email`, `wp_unslash`, and `sanitize_text_field`.
6. Updated date output in email template to use `gmdate()` instead of `date()` to comply with VIP timezone standards.
7. Removed dependent IP logging via `REMOTE_ADDR` to eliminate VIP linting errors and improve compliance.

Verification:
- PHP_CodeSniffer ran with WordPress-VIP-Go standard.
- Result: 0 ERRORS, 1 WARNING (wp_mail sparingly usage).
- Manual check: Subscription Flow (PHP version) and GraphQL integration are working correctly.
